{
  "name": "teamup-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types package scaffold for Teamup.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "node --test",
    "lint": "tsc -p tsconfig.json --noEmit --pretty false",
    "clean": "tsc -b --clean"
  }
}
